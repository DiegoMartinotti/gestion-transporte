/**
 * Punto de entrada principal de la aplicación
 * Este archivo simplemente importa y ejecuta server.ts para mantener compatibilidad
 * con scripts existentes que puedan estar usando index.ts como punto de entrada.
 */

// Importar server.ts (que contiene toda la lógica de la aplicación)
import './server';

// No exportamos nada, ya que la aplicación se inicia directamente